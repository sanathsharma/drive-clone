/**
 * Deep modules with business logic that makes use of models or db directly for queries should not be made directly
 * outside of the module. See README.md for the convention and how it's enforced.
 */
